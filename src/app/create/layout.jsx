export const metadata = {
  title: "Create-Routine",
  description: "Create Routine for Users",
};

const CreateRoutineLayout = ({ children }) => {
  return <>{children}</>;
};
export default CreateRoutineLayout;
